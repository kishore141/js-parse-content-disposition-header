const TYPE_INLINE = 'inline';
const TYPE_ATTACHMENT = 'attachment';
const TYPE_UNKNOWN = 'unknown';
const FILENAME = "filename";
const FILENAME_SEPARATOR = '"'
const FILENAME_ENCODED = "filename*";
const FILENAME_ENCODED_PREFIX = "filename*=utf-8''";

export = {
    TYPE_INLINE,
    TYPE_ATTACHMENT,
    TYPE_UNKNOWN,
    FILENAME,
    FILENAME_SEPARATOR,
    FILENAME_ENCODED,
    FILENAME_ENCODED_PREFIX
}